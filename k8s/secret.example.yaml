# Example Secret template — DO NOT commit real credentials.
#
# Option A: edit placeholders below, save as secret.yaml (gitignored), then apply.
# Option B (recommended): create the secret from the CLI:
#   kubectl create secret generic triageiq-secrets \
#     --from-literal=jwt-secret='your-strong-random-secret'
#
# This example file is NOT applied by default.
apiVersion: v1
kind: Secret
metadata:
  name: triageiq-secrets
  labels:
    app: triageiq
type: Opaque
stringData:
  jwt-secret: REPLACE_WITH_A_STRONG_RANDOM_VALUE
